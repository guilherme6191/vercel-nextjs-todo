import { TodoButton } from './button';
import { TodoFormRoot } from './root';
import { TodoInput } from './todo-input';

export const TodoForm = {
  Button: TodoButton,
  Input: TodoInput,
  Root: TodoFormRoot,
};
